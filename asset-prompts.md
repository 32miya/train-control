# 指令室ゲーム - グラフィック生成プロンプト集

他のAI画像生成ツールに1パーツずつ描かせるための仕様書です。各項目は単体でコピペして使える完結したプロンプトになっています。

## 重要な注意事項（必読）

1. **1パーツ＝1ファイルで出力させること。** 複数パーツをまとめた見本シート（コンタクトシート）は絶対に避けてください。1枚のモックアップ画像を後から切り抜いて使う方式は、個々のパーツの解像度が足りず、ゲーム内で拡大するとぼやけてしまうことが判明しています。
2. **指定したピクセルサイズぴったり、またはその整数倍（2倍・4倍）で出力させること。** 中途半端に小さい画像を引き伸ばして使うと同じ問題が起きます。迷ったら大きめに生成してもらい、後でこちらで縮小します。
3. アイコン・スプライト系は**背景透過PNG（アルファチャンネルあり）**を指定してください。背景パネル・路線図は不透明で構いません。
4. 生成後は必ずファイル名を下記の通りにしてください（コード側の参照名と一致させるため）。
5. 各プロンプトの最後に **ネガティブ指示** を付けています。対応していないツールでは本文に「〜は避けること」として溶け込ませてください。

---

## 共通スタイル定義（すべてのプロンプトの先頭に付けてください）

```
近未来的な鉄道総合指令所のUI素材。ダークネイビー基調（#0a0e1a〜#121a2e）、
シアン〜ブルーのネオン発光ライン、ガラス質感のパネル、精密な計器・モニター類。
写実寄りだがゲームUIとして視認性の高い、クリーンでコントラストの効いた質感。
光源は画面奥からのモニター光と、パネル自体の発光ラインの2種類のみ。
過度な装飾やノイズは避け、主題物がはっきり視認できる構図にすること。
```

English (better results on most generators):
```
Sci-fi railway control-room UI asset. Dark navy base (#0a0e1a-#121a2e),
cyan-blue neon accent lines, glass-panel texture, precise gauges and monitors.
Semi-realistic but clean and high-contrast for game UI legibility.
Lighting comes only from screen glow and the panel's own neon edge-lighting.
Avoid excessive noise/decoration; subject must read clearly at small sizes.
Flat front-on / orthographic framing, no perspective distortion, no camera tilt.
```

---

## A. 部署パネル背景（5種 × 通常時/警告時 = 10枚）

**サイズ: 600×320px／不透明PNG**（ゲーム内表示は300×160なので2倍で生成）
**ファイル名: `dept_<id>_normal.png` / `dept_<id>_warn.png`**

### 共通レイアウト指示（全5種に適用）
- 画面を左右2分割：**左40%にメインアイコン**（円形または角丸パネルの中に単一モチーフ、パネル全体の縦の60%程度を占める大きさ）、**右60%に補助的な計器・モニター類**（数字や波形、リスト等、読める必要はないので装飾でよい）
- 四隅に軽いビネット（暗め）を入れ、中央のアイコンに視線が集まるようにする
- normal版とwarn版は**同一構図**にし、色調と発光状態だけを変える（構図が変わると見比べたときに違う部署に見えてしまうため）
- テキストは書き込まない（部署名はゲーム側でCSS表示するため）

### A-1. 輸送指令（transport）
```
左に丸いブルーの発光パネルの中に、通勤電車を正面から見た顔（先頭車両）の
シンプルなピクトグラム。右側に古典的な発車時刻表ボード（フリップ式、
文字は判読不要のダミー）を薄暗く配置。
【warn版】発光パネルの縁が赤く点滅している状態、電車ピクトグラムの背景が
赤色に切り替わり、右下に小さな警告三角アイコンを追加。
ネガティブ: 文字を書き込まない、人物を描かない、派手すぎる反射を入れない。
```

### A-2. 信号指令（signal）
```
左に丸いグリーン発光パネルの中に、信号機とポイント（分岐器）レールを
上から見た簡略アイコン。右側に複数の小さな監視モニターを並べ、緑色の
ランプが等間隔に点灯している様子。
【warn版】ランプの一部が赤く点滅し、信号アイコン自体も赤枠に変化。
ネガティブ: リアルすぎる鉄道写真調にしない、文字は書き込まない。
```

### A-3. 電力指令（power）
```
左に丸いオレンジ〜アンバー発光パネルの中に、変電所の鉄塔または
配電盤をシンプルにデフォルメしたアイコン。右側に電流計メーター
（針が中央付近を指す）を2〜3個並べる。
【warn版】メーターの針が右端（危険域）に振れ、パネル全体が赤くにじむ
発光に変化。
ネガティブ: 実写の変電所写真にしない、火花や炎を描かない（これは
トラブルアイコン側の役割のため）。
```

### A-4. 運用指令（crew）
```
左に丸いパープル発光パネルの中に、乗務員の帽子とバッジ、または
カレンダー＋チェックマークをモチーフにしたシンプルなアイコン。
右側に縦書きのシフト表・点検リストのモニター（文字は判読不要の
ダミーライン）。
【warn版】リストの一部の行が赤くハイライトされ、パネル縁が赤発光。
ネガティブ: 具体的な人物の顔を描かない、文字は書き込まない。
```

### A-5. 旅客指令（passenger）
```
左に丸いピンク〜マゼンタ発光パネルの中に、拡声器（メガホン）または
案内板ピクトグラムのアイコン。右側に電光掲示板（緑色のドットマトリクス
風パターン、文字は判読不要のダミー）。
【warn版】電光掲示板が赤文字表示に切り替わり、パネル縁も赤発光。
ネガティブ: 具体的な文章を書き込まない（ダミーの光点パターンのみ）。
```

---

## B. トラブルアイコン（7種）

**サイズ: 256×256px／背景透過PNG**
**ファイル名: `trouble_<id>.png`**

### 共通レイアウト指示
- 中央に角丸正方形または円形の発光フレーム（直径・一辺とも全体の80%程度）
- フレームの中央60%にモチーフ1つだけを大きく配置。細部を詰め込みすぎない（縮小してもシルエットで判別できることが最優先）
- フレームの外側は完全透明（余白を大きく取りすぎない、モチーフとフレームで画面いっぱいに使う）

### B-1. 人身事故（accident）
```
角丸パネルの中央に、燃え上がる炎のシンプルなアイコン。色はオレンジから
赤へのグラデーション、中心に近いほど白っぽく明るい。緊急性・危険性を
感じる鋭いシルエット。
ネガティブ: 人物やホーム・線路を描き込まない（抽象的な炎のみ）、煙は
最小限に。
```

### B-2. 落雷（lightning）
```
角丸パネルの中央に、鋭くジグザグに折れた稲妻のアイコン。色は白〜シアン
のグラデーション、周囲にごく薄い青白い発光をにじませる。
ネガティブ: 雲や空の背景を描かない（稲妻の形状のみ）。
```

### B-3. 強風・大雨（storm）
```
角丸パネルの中央に、丸みのある雲のシルエットから斜めの雨粒が数本
落ちているアイコン。色は薄い水色〜白。雲の輪郭に薄い発光線。
ネガティブ: 稲妻を含めない（落雷アイコンと役割が重複するため）、
背景の風景は描かない。
```

### B-4. 車両故障（train_trouble）
```
角丸パネルの中央に、スパナ（レンチ）1本を斜めに配置したシンプルな
アイコン。色はグレー〜白の金属的な質感、軽いハイライト。
ネガティブ: 電車本体や複雑な機械部品は描かない（工具のシルエットのみ）。
```

### B-5. 分岐器不転換（point_failure）
```
角丸パネルの中央に、1本の線がY字に2方向へ分岐する矢印アイコン。
色はオレンジ〜アンバー、分岐点が明るく発光。
ネガティブ: 実際の線路のレール断面などリアルな描写にしない、矢印の
抽象的な形状に留める。
```

### B-6. 駆け込み乗車（daily_minor）
```
角丸パネルの中央に、走っている人物の白いピクトグラム（空港・駅の
案内サインのような簡略シルエット）。前傾姿勢でスピード感を出す。
色は白〜ライトグレー単色。
ネガティブ: 顔の表情や服装の詳細は描かない（シルエットのみ）。
```

### B-7. 乗務員手配切れ（crew_shortage）
```
角丸パネルの中央に、腕時計または懐中時計のシンプルなピクトグラムと、
その右下に小さく人物シルエットを重ねた組み合わせアイコン。「時間切れ」
の緊張感を表現。色はパープル〜バイオレット系。
ネガティブ: 文字（数字の12,3,6,9以外）は書き込まない、複雑な背景は
描かない。
```

---

## C. 電車側面スプライト（2種）

**サイズ: 512×256px／背景透過PNG**
**ファイル名: `train_<id>.png`**

### 共通レイアウト指示
- 真横から見た側面図（オルソグラフィック、パースをつけない）
- 2〜3両編成、車両同士は連結部分が自然につながって見えるように
- **必ず左向き**（先頭車両が画面左側）に統一。ゲーム内で反転もするが、元絵の向きを揃えておくと反転時に不自然にならない
- 画像の上下左右に余白を持たせすぎない。車両が横幅いっぱいに近い形で収まるようにする

### C-1. 中央線風車両（chuo）
```
オレンジ色の帯（ラインカラー）が入った通勤形電車の側面図、2〜3両編成、
左向き。窓とドアが規則的に並ぶ、屋根はやや丸みのある形状。
車体色はシルバー〜ホワイトベース、帯だけがオレンジ。
ネガティブ: 車両の前面（顔）は描かない（純粋な側面図）、背景・線路・
影は描かない（背景は完全透明）。
```

### C-2. 山手線風車両（yamanote）
```
グリーン色の帯（ラインカラー）が入った通勤形電車の側面図、2〜3両編成、
左向き。窓とドアが規則的に並ぶ、屋根はやや丸みのある形状。
車体色はシルバー〜ホワイトベース、帯だけがグリーン。
ネガティブ: 車両の前面（顔）は描かない（純粋な側面図）、背景・線路・
影は描かない（背景は完全透明）。
```

---

## D. 中央路線図背景

**サイズ: 1600×600px／不透明PNG**
**ファイル名: `route_map.png`**

```
指令室の大型表示盤に映し出された路線図のイメージ。横長の構図で、
2本の路線（オレンジ線が上寄り、グリーン線が下寄り）がほぼ水平に
画面を横断する。左右どちらかの位置1〜2箇所で、路線同士が交差する
乗換駅を配置する。乗換駅は二重リングの発光マーカー（direkt目立つ
サイズ）、それ以外の一般駅は小さな白い点で表現する。

背景はダークネイビーの地に、薄いグリッド線または回路基板のような
装飾ラインを敷き詰め、「大型モニターに投影された地図」であることが
伝わるようにする。画面の四隅は自然にビネット（暗く）してよい。

構図上の注意: この画像は横幅を可変で引き伸ばして使うため、
主要な路線・駅マーカーは画面の中央60%程度に収め、左右端付近には
単調な背景（グリッドのみ）が続くようにすること。

ネガティブ: 実在の駅名・地名の文字は書き込まない、日本語・英語の
テキストラベルは一切入れない（ゲーム側で重ねないため無地でよい）。
```

---

## E. 指令発令ボタン（3状態）

**サイズ: 800×240px／背景透過PNG可**
**ファイル名: `btn_normal.png` / `btn_pressed.png` / `btn_disabled.png`**

### 共通レイアウト指示
- 横長の角丸長方形ボタン1個のみ。画像いっぱいにボタンを配置し、周囲の余白は最小限
- **ボタン内に文字は一切描かない**（「指令発令」の文字はゲーム側のCSSで重ねて表示するため、文字を描き込むと二重表示になる）
- 3状態は同じ形・同じアングルで、色と質感だけを変える

### E-1. 通常・有効（normal）
```
鮮やかなブルー（#4fc3f7〜#2196c9のグラデーション）の角丸長方形ボタン。
上部にハイライトの光沢（ガラスのような反射）、下部はやや暗いブルーに
沈む。縁にごく薄い発光の縁取り。文字は入れない。
```

### E-2. 押下時（pressed）
```
normalと同じ形・同じ色相だが、全体的に一段暗く彩度を落とし、
上部の光沢ハイライトを弱める。ボタンが少し沈み込んだような
質感（内側に軽いシャドウ）。文字は入れない。
```

### E-3. 無効（disabled）
```
彩度を大きく落としたグレー（#3a4258程度）の角丸長方形ボタン、
光沢・発光なし。全体的にフラットで「押せない」印象を与える質感。
文字は入れない。
```

---

## F. エフェクト（任意・余裕があれば）

**サイズ: 512×512px／背景透過PNG（1コマ静止画）**
**ファイル名: `fx_spark.png` / `fx_lightning.png` / `fx_rain.png`**

### F-1. 火花（spark）
```
中央から放射状に飛び散るオレンジ〜白の火花パーティクル1コマ。
中心が最も明るく白に近く、外側に向かってオレンジ〜赤に減衰する。
背景は完全透明、火花の粒だけが画面に浮いている状態。
```

### F-2. 雷フラッシュ（lightning）
```
画面を縦に貫く、鋭くジグザグに折れた稲妻の閃光1コマ。中心の光の
筋は白、その周囲にシアン〜青のグロー（発光にじみ）。背景は完全透明。
```

### F-3. 雨タイル（rain）
```
斜め方向に降る雨粒のストリーク（線状の軌跡）を敷き詰めたパターン。
左右の端をつなげてタイル状に繰り返し表示できるよう、左端と右端の
模様が連続するようにする。色は薄い水色〜白、半透明。背景は透明。
```

---

## 生成後の受け渡し

生成した画像は `~/Workspace/games/train-control/assets/` に上記ファイル名で保存してください。
揃ったものから順次コードに組み込みます（全部揃うまで待つ必要はありません）。
